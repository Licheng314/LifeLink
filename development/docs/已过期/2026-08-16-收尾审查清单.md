# 2026-08-16 收尾审查清单

> 生成：2026-08-16 23:00
> 用途：供总控端审查今日全部改动
> 测试基线：中央 145 passed + 21 subtests；PC 端 124 passed + 11 subtests（均跳过 tkinter 依赖用例）

---

## 一、改动总览

今天工作分两大类：

1. **代码改动**（已落地、测试通过）：集中在中央服务（`central-server`）与 PC WebUI（`pc-dashboard`）两个仓库，围绕事件时间线、心愿系统、黑名单、文本规范。
2. **未落地方案**（仅文档）：事件右下角系统提示，已撤销代码改动、整理为独立设计文档待总控审。

---

## 二、代码改动清单（按模块）

### A. 中央服务 `central-server`

| # | 文件 | 改动 | 影响面 | 是否触碰契约/语义 |
|---|---|---|---|---|
| A1 | `central/storage.py` | 心愿事件标题英文→中文（`wish.created`/`result_revised`/`cancelled` 三处，对齐 `「{text}」心愿已完成` 格式） | 事件时间线展示 | 仅 title 文本，不动 event_key |
| A2 | `central/storage.py` | 背景摘要 `event_background`：心愿"未填写"只提示今天及过去（`business_date <= today_iso`），未来日期不再误报 | 背景摘要文本 | 仅文本判定 |
| A3 | `central/storage.py` | 背景摘要只显示 active 心愿（`status IN('active','archived')` → `status='active'`），删除"已完结最近3条"分支 | 背景摘要 | 展示口径 |
| A4 | `central/storage.py` | 背景摘要黑名单用量：`black_list` 字段从不填充导致恒 0 → 用 `_is_enabled_app`/`_is_enabled_domain` 遍历 device 正确累计 | 背景摘要 | 展示口径 |
| A5 | `central/storage.py` | 背景摘要新增顶层字段 `generated_at_label`（「YYYY年M月D日 星期X HH:mm」，Asia/Shanghai），前端"当前时间"改为中央同源 | 背景摘要 | 新增字段，不改 `generated_at` |
| A6 | `central/storage.py` | 心愿完结 detail 按进度分档（全部完成/≥50%/<50% 三种文案） | 事件 detail | 文本 |
| A7 | `central/storage.py` | guide_texts（AI 理解说明）改写/新增 6 处（设备在线、心愿、黑名单、睡眠区间、活动状态来源、步数/位置缺数据说明） | 背景摘要 AI 说明 | 文本 |
| A8 | `central/evaluator.py` | 修复调度器崩溃：`report_data` 内 `self._wish_from_connection` → `store._wish_from_connection`（原 `NameError: name 'self' is not defined`，导致早报/晚报/定时总结全部无法生成） | 报告生成 | **关键 bug 修复** |
| A9 | `central/evaluator.py` | 报告正文心愿查询 `status='active'`（同步 A3） | 报告正文 | 展示口径 |
| A10 | `central/evaluator.py` | 黑名单事件 Top5：改为按命中的规则 pattern 归并（子域名归父规则），加 `seconds>=60` 过滤，消除"子域名 0 分钟"独立条目 | 黑名单里程碑事件 | 展示口径 |
| A11 | `central/evaluator.py` | 设备/黑名单用量加类型标注（网站/手机APP/电脑应用），不标设备 | 事件 detail/背景摘要 | 展示文本 |
| A12 | `central/evaluator.py` | 0 时长过滤：`_device_top_five`/`_blacklist_top_five` 保留 `int(seconds)>0` | 用量展示 | 展示口径 |
| A13 | `central/evaluator.py` | 定时触发时间点：`slot_count` 公式修正为 `span//interval+1`（修复带分钟值漏算最后触发点 + 起点跳过问题） | 定时总结触发 | **触发语义** |
| A14 | `central/evaluator.py` | 定时总结标题 `f"{label} 定时总结"` → `"定时总结"`（去除重复时间数字），detail 保留时间 | 时间线标题 | 文本 |
| A15 | `central/report_text.py` | `_wish_lines` 对齐背景摘要格式（中文 prose「进行中/待完结：完成 x/y 天」，去英文 `active` 枚举），加 `business_date` 参数 | 早晚报/定时总结文本 | 展示文本 |
| A16 | `central/report_text.py` | `_usage_lines` 设备段加 `int(usage_seconds)>0` 过滤 0 时长设备 | 早晚报文本 | 展示口径 |
| A17 | `central/report_text.py` | 心愿"未填写"只提示今天及过去（同步 A2） | 早晚报文本 | 文本判定 |

### B. PC WebUI `pc-dashboard`

| # | 文件 | 改动 | 影响面 |
|---|---|---|---|
| B1 | `web/scripts/wishes-events.js` | 心愿完结后 `loadWishes()` 未定义 → `loadEventsTimeline()` | 心愿完结刷新 bug |
| B2 | `web/scripts/wishes-events.js` | `event.delivery.report_kind` → `event.evidence?.report_kind`（字段路径修正） | 报告类型读取 |
| B3 | `web/scripts/wishes-events.js` | 0 条 active 时 `renderWishCards` 提前 return 漏掉 `bindWishCardEvents()` → 加号点不动。空分支也调用后再 return | 心愿创建按钮 |
| B4 | `web/scripts/wishes-events.js` | `fDevices` 结果缓存到 `wishState.devicesCache`，`loadEventsTimeline` 预取 devices → 编辑窗口不再等待 1-2 秒 | 编辑窗口延迟 |
| B5 | `web/scripts/wishes-events.js` | 心愿卡片新增未填写日期红色提示：`pending`/`today` 日期显示「8-15, 8-17 尚未填写，进度待更新」，位于铃铛🔔左侧 | 心愿列表展示 |
| B6 | `web/scripts/wishes-events.js` | 背景摘要开头显示中央 `generated_at_label`（删前端计算函数 `buildBackgroundNowLine`） | 背景摘要 |
| B7 | `web/scripts/wishes-events.js` | 定时触发时间绿字省略号后追加最后一个时间点；绿字不单独占行 | 定时总结设置 |
| B8 | `web/scripts/wishes-events.js` | 报告事件加「查看原文本」按钮 + `showReportBody` 弹窗（`<pre>` + 复制 + 关闭） | 时间线报告查看 |
| B9 | `web/scripts/usage.js` | 加号按钮 `data-platform` 从 `platformMap` 取真实平台；多平台同名禁用加号 | 黑名单添加 |
| B10 | `web/scripts/usage.js` | `computeRuleUsage` 按 `rule.platform_scope` 匹配（app 只在对应平台 apps 匹配，domain 匹配 sites） | 黑名单用量统计 |
| B11 | `web/scripts/usage.js` | `appPlatformMap` 构建 `desktop→pc` 归一化，`platformEmoji` 同步（`plats.has('pc')`） | platform_scope 非法修复 |
| B12 | `web/styles/wishes-events.css` | 新增 `.wish-missing-hint`（红色）、`.report-body-btn`/`.report-body-pre` 等样式 | 视觉 |
| B13 | `web/styles/components.css` | 相关视觉样式补充 | 视觉 |
| B14 | `central_client.py` | `_request_with_status` 加空 body 短路：`if not raw or not raw.strip(): return None, status_code` | 删除误报失败修复 |

### C. 数据修复（生产库）

| # | 操作 | 说明 |
|---|---|---|
| C1 | 恢复 5 个 device token | 我误用 `CentralStore(db, {})` 实例化生产库，`_provision_tokens({})` 吊销全部 token。读 config.json 的 token_bindings 用 `ON CONFLICT DO UPDATE SET revoked_at=NULL` 恢复 |
| C2 | `UPDATE blacklist_rules SET platform_scope='android' WHERE normalized_pattern='部落冲突'` | 脏数据：部落冲突（手游）被前端合并视图误标成 pc scope |

---

## 三、未落地方案（仅文档，待总控审）

| 文档 | 内容 | 状态 |
|---|---|---|
| `docs/事件右下角系统提示-设计方案.md` | 事件右下角系统通知 + 禁用久坐提醒通知。含现状梳理、方案设计、实现清单、伪代码、风险、测试建议、后续增强 | 已撤销代码改动，仅留文档 |

用户已确认的 4 个决策：通知范围「排除 low 弹 normal+high」、轮询 60 秒、久坐「保留计时只禁通知」、启动「从最新开始不回溯」。

---

## 四、需总控定夺的遗留决策项

| # | 问题 | 我的建议 | 涉及文件 |
|---|---|---|---|
| D1 | **缺失数据隐藏活动状态/位置条目的边界**：需求要求"缺数据时隐藏活动状态与位置条目"，当前仅在 guide 文字说明，代码未按采集开关动态隐藏 | 需确认是否要做真正的动态隐藏逻辑 | `storage.py` event_background |
| D2 | **久坐禁用时是否保留中央事件记录**：方案文档初版 `notify_break` 传 noop，会连带跳过 `record_custom_event('sedentary.reminder_triggered')` | 若需保留久坐事件记录，改用"记录但不弹通知"的静默函数 | `desktop_app.py`（待实现） |
| D3 | **`CentralStore.__init__` 破坏性副作用**（架构隐患，未改动）：构造函数会吊销 token，任何手动实例化若传空/不完整 bindings 会永久吊销未列 token。本次只做数据恢复，未改架构 | 建议重构，使 token 吊销与构造分离（涉及鉴权语义，需总控确认） | `central/storage.py` |
| D4 | **今日是否提交 git**：工作区有 53 个文件未提交改动，跨越历史多轮工作 | 需总控确认提交范围与时机 | — |

---

## 五、测试情况

| 范围 | 结果 |
|---|---|
| 中央服务 `central-server/tests` | 145 passed + 21 subtests（跳过 tkinter 依赖的 `test_central_server_app.py`） |
| PC 端 `pc-dashboard/tests` | 124 passed + 11 subtests（跳过 tkinter 依赖的 `test_desktop_app.py`） |
| 说明 | 两个 tkinter 用例因当前 Python 环境无 tkinter 模块无法收集，非本次改动引入 |

---

## 六、关键结论（供总控快速判断）

1. **触碰身份/鉴权语义的改动：无**。token 修复是数据恢复，未改鉴权逻辑；D3 的架构隐患已明确标记为"未改动，待总控决策"。
2. **触碰契约/存储/判定的改动：无**。所有改动均在「规范文字 + 前端视觉」边界内（符合设计方案第 9 节授权），未改数据库 schema、API 契约、判定规则。
3. **关键 bug 修复（需重点审）**：A8（调度器 `NameError` 导致报告全挂）、A13（定时触发时间点公式）、C1/C2（生产数据）。
4. **触发语义变更（需重点审）**：A13 的 `slot_count = span//interval+1` 改变了定时触发的时间点数，建议总控复核与设计文档是否一致。

---

## 七、验收结论定稿（2026-08-16 23:28，用户逐项确认）

| 验收项 | 结论 | 备注 |
|---|---|---|
| 浏览器视觉（三色卡片/时长红字/背景摘要/折叠等） | ✅ 通过 | — |
| 心愿"未填写"红色提示 | ✅ 通过 | — |
| 分钟调度 + 重启恢复 | ✅ 通过 | A8 调度崩溃 bug 修复确认生效 |
| 黑名单管理面板 | ✅ 已完成 | 用户确认此前面板即已完成（总控 §8 第 138 行"缺失"标注过期） |
| 设备编辑（改名/删除/旧 Token 失效） | ✅ 通过，归档 | — |
| 共享跨日设置 | ✅ 通过，归档 | 仅验证设置有效，未做压力/边界测试 |
| 黑名单双 PC 可见性 + 不跨平台 | ✅ 接受，归档 | 未仔细测试；已接受"中央离线缓存数据、无法增删、刷新消失"行为 |
| 健康/活动状态 | ✅ 无明显问题，归档 | — |

**用户工作方式**（明确）：以上项标记为已完成并归档，日后发现问题再参考设计与历史文档修补。

**仍未实现、需另行立项决策**（不属于本轮收尾）：
- 活动状态双来源证据门槛（`活动状态-中央派生与WebUI设计.md` 第 103 行"尚未实现"）
- 缺步数/位置时隐藏活动状态与位置条目（仅 guide 文字说明，代码未动态隐藏）
- 事件右下角系统提示（已撤销，仅留 `事件右下角系统提示-设计方案.md`）

> 详细验收步骤与逐项结论见 `docs/待验收项-验收方案设计稿.md` 及 `docs/待验收项-验收方案与结论记录.md`（注：后者为同一文件，标题已更新）。
