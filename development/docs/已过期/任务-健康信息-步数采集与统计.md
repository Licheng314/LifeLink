# 任务：健康信息——步数采集与统计

> **已归档（2026-08-16）**：本任务单对应的功能已完成并经用户验收/接受式验收，仅作历史追溯，不作为当前任务或验收结论。验收记录见 2026-08-16-待验收项验收方案与结论记录.md。

```yaml
任务名称: Android 步数可靠采集与中央步数统计引擎
用户目标: 修复手机无法采集步数，并形成可上传、可重试、可由中央按设备和日期统计的步数事实
风险等级: 大型（新增原始事件语义、Android 权限和后台采集）

执行配置:
  建议模型: gpt-5.6-terra
  推理强度: medium
  继承上下文: none

必须阅读:
  - docs/README.md
  - docs/项目总控.md
  - docs/健康信息-后端数据设计.md
  - contracts/README.md
  - mobile-app/README.md
  - mobile-app/HEALTH.md
  - central-server/README.md

允许修改:
  - mobile-app/app/src/main/AndroidManifest.xml
  - mobile-app/app/src/main/java/com/liferadio/sync/data/local/ 中步数、Room 与设置相关文件
  - mobile-app/app/src/main/java/com/liferadio/sync/data/remote/CentralEventMapper.kt
  - mobile-app/app/src/main/java/com/liferadio/sync/service/SyncService.kt
  - mobile-app/app/src/main/java/com/liferadio/sync/ui/ 中仅为 ACTIVITY_RECOGNITION 授权、启用状态和诊断所必需的最小 UI
  - mobile-app/app/src/test/ 下步数专项测试
  - central-server/central/health_steps.py（新建独立纯计算模块）
  - central-server/tests/test_health_steps.py（新建专项测试）
  - mobile-app/HEALTH.md 中步数当前实现说明

明确不做:
  - 不修改 contracts/、central-server/central/domain.py、http.py、storage.py、read_model.py
  - 不实现统一 /v1/health-info 路由；由总控整合，既有 /v1/health 存活检查保持不变
  - 不修改 PC 或 WebUI
  - 不实现睡眠算法、健康评分、提醒、Health Connect 或手环接入
  - 不生成 APK，不把 JVM 测试当真机验收
  - 不覆盖、回滚或格式化当前工作区其他未提交文件

数据与契约影响:
  权威数据来源: Android TYPE_STEP_COUNTER 原始累计观察；中央只在同设备同 session 内差分
  是否修改共享契约: 否，由总控统一整合
  是否迁移或删除数据: 否
  是否影响认证或同步: 使用现有 Room/outbox/逐事件 ACK；不得改变确认语义

验收标准:
  - Android 10+ 正确声明、检查并请求 ACTIVITY_RECOGNITION
  - 用户拒绝后保持可解释状态，不循环申请
  - 后台同步服务能够定期取得计步器当前累计值并持久化不可变观察
  - 计数下降或重启会生成新的 counter_session_id
  - 样本和 session 元数据持久化，进程退出后可恢复
  - 映射为待整合事件 health.steps_observation，事件 ID 对同一观察稳定
  - 中央纯计算模块按设备/日期差分，覆盖 session 重置、无效负差、多设备不求和

测试分工:
  执行代理运行:
    - Android 步数 JVM 专项测试
    - central-server/tests/test_health_steps.py
  总控验收运行:
    - 契约、中央、Android 必要回归和关键反例

交付要求:
  - 修改文件
  - 实际行为变化
  - 测试数量与结果
  - 未完成风险
  - 明确列出总控仍需完成的 contract/domain/http 整合点
  - 真实手机必须验证权限、传感器可用性、后台采样和步数递增
```
