# 任务：统一用户数据路径

状态：代码、数据迁移与本机真实启动验收已完成。

- 目标：所有安装副本共享当前 Windows 用户唯一的 `LifeLink` 数据根目录，并保留原 PC 客户端身份。
- 不做：不更改协议、事件口径、APK 数据路径或程序内部旧类名。
- 修改范围：中央配置路径、PC 配置/身份/运行数据路径、MCP 状态路径、对应测试与现役 README。
- 数据权威：`%USERPROFILE%\LifeLink\central` 是中央权威库；`%USERPROFILE%\LifeLink\client` 是 PC 唯一身份与队列。
- 验收：源码与发行包解析到相同路径；原身份和中央库迁移完整；打包试验身份被撤销且本地状态删除；专项测试通过。
- 风险：高。迁移前建立可恢复备份，不合并两个客户端 outbox；中央历史事实只在用户明确授权后删除。

实际迁移保留原客户端 `desktop-90f40c44-…`。用户明确授权后，试验打包设备的 92 条事件、9 个批次、2 条邀请、2 条时间线及身份凭据已从中央库删除；完整迁移前备份保留在 `%USERPROFILE%\LifeLink\backups`。
