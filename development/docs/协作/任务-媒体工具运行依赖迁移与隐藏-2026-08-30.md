# 任务：媒体工具运行依赖迁移与隐藏

```yaml
任务名称: 媒体工具运行依赖迁移与隐藏
用户目标: 将本机 Bilibili 音频工具的二进制依赖迁入统一 LifeLink 用户数据目录，并暂时不在 PC WebUI 展示该小工具。
风险等级: 中型

允许修改:
  - central-server/central/media.py
  - development/tools/bilibili-audio/
  - pc-dashboard/dashboard.html
  - pc-dashboard/web/README.md
  - development/docs/协作/任务-媒体工具运行依赖迁移与隐藏-2026-08-30.md

明确不做:
  - 不删除音频提取后端 API 或既有 MP3 文件。
  - 不实现首次使用自动下载依赖。
  - 不修改共享契约、设备身份、中央数据库或同步链路。

数据与契约影响:
  权威数据来源: 中央服务数据根目录及统一用户工具目录。
  是否修改共享契约: 否
  是否迁移或删除数据: 是，仅移动三个可再下载的本机工具二进制文件。
  是否影响认证或同步: 否

验收标准:
  - 中央服务调用工具时使用 %USERPROFILE%\\LifeLink\\tools\\bilibili-audio\\bin。
  - 原 development/tools/bilibili-audio/bin 不再存放二进制文件。
  - WebUI 不再显示“小工具”导航与页面，其他页面可正常加载。
```

状态：代码完成，自动化/专项校验通过；未进行真实音频下载验收（入口已按需求隐藏）。

执行结果：

- 已将本机 `ffmpeg.exe`、`ffprobe.exe`、`yt-dlp.exe` 从开发目录移动到
  `%USERPROFILE%\\LifeLink\\tools\\bilibili-audio\\bin`。
- 中央服务启动下载子进程时显式传入该唯一运行时目录；命令行工具与手动安装脚本也使用相同位置。
- WebUI 已隐藏“小工具”导航、页面展示、样式与脚本加载；后端媒体 API 和源码未删除。
