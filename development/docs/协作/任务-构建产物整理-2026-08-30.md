# 任务：构建产物与开发目录整理

状态：代码、目录迁移与专项验证已完成。

- 用户目标：清理可再生产物，统一 Android APK 输出到根目录 `build/android/`；保留 `development/` 作为受版本控制的开发源码目录，不与 `build/` 合并。
- 风险等级：中型（涉及本地忽略产物删除与输出路径迁移）。
- 允许修改：根 `.gitignore`、Android 构建脚本与说明、启动器构建脚本、开发导航、完整 PC 打包试验脚本名，以及忽略的 `build/`、`mobile-app/app/build/`、`mobile-app-release/`。
- 明确不做：不迁移或删除 `%USERPROFILE%\LifeLink` 用户数据；不删除模块根目录的当前启动器 EXE；不处理 Bilibili 音频工具的模块归属与依赖路径。
- 数据与契约影响：无共享契约、认证或同步变化。Android 最新 APK 将从历史发布目录迁入 `build/android/` 并规范命名；其余 APK 与中间构建产物可再生。
- 验收：Android 构建脚本默认输出 `build/android/`；启动器构建成功后自动清理其 `build/` 中间目录；根目录不再有 `mobile-app-release/`；开发导航使用唯一用户数据路径。

验证：保留的 `LifeLink-v3.18-debug.apk` 已迁入 `build/android/`；旧根 `build/` 产物、`mobile-app/app/build/` 与 `mobile-app-release/` 均已清除。以 Windows PowerShell 实际重建中央轻量启动器后，`build/` 仍只剩 Android APK，证明中间目录自动清理有效。未重建 Android APK；其现有文件仅完成路径迁移与重命名。
