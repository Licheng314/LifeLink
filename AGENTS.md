# Life Link Agent Rules

本文档为原始开发者给AI编辑项目的基础约束。如果有其他AI想进行项目修改，注意提醒一下你的用户。不要引入与你无关的约束。
所有 AI 开始工作前，先阅读 `development/docs/README.md`，再按其索引读取项目总控、任务专项规范、共享契约和对应模块 README。聊天记录与 `.workbuddy` 记忆只能作为历史线索，不能覆盖当前文档、契约和代码事实。

## 默认调度

- 小型任务（通常不超过 2 个文件、无契约/存储/权限变化）由当前代理直接完成，不主动创建子代理；用户明确要求子代理时除外。
- 常规执行代理默认使用 `gpt-5.6-terra`：机械修改和简单测试用低推理，明确的跨文件实现用中等推理。
- `gpt-5.6-sol` 主要用于架构决策、数据安全、复杂故障和最终高风险审查。
- 子代理默认不继承完整聊天历史。优先使用 `fork_turns: "none"`，通过独立任务单和项目文档提供上下文；确需最近对话时只传最少轮次。
- 用户指定模型、推理强度、上下文或协作方式时，以用户要求为准。

## 上下文与范围

- 每个执行任务必须先明确：目标、不做事项、允许修改的文件、验收标准和风险等级。
- 只读取任务直接相关的文档和代码，不为局部修改扫描全部历史记录。
- 不顺手修复范围外问题；把发现记录到交付报告，由总控决定是否立项。
- 共享工作区可能包含用户或其他代理的未提交修改，不覆盖、不回滚、不批量格式化无关文件。

## 验证分工

- 执行代理负责新增或运行专项测试，使用精简输出。
- 总控/验收代理负责检查实际差异，并原则上只运行一次必要的全量回归。
- 不由执行代理和验收代理重复打印同一批冗长成功日志；汇报测试数量、结果和失败项即可。
- “代码完成”“自动化测试通过”“真实设备验证通过”必须分开表述。

## Windows 启动脚本

- 所有 `.bat` / `.cmd` 必须是纯 ASCII、CRLF 换行，并由 `.gitattributes` 固定 `eol=crlf`；不要依赖 `chcp 65001` 在批处理中承载中文。
- BAT 只做薄启动桥：解析绝对路径、使用完整引号调用 PowerShell/Python、传递退出码，并在双击启动失败时保留窗口；复杂流程和用户中文提示放入 PowerShell 或 Python。
- 涉及带空格的项目路径时，不拼接裸命令字符串，不让 BAT、PowerShell、CMD 之间重复转义同一路径。
- 诊断“PowerShell 闪退”时，先用 `cmd.exe /d /c call <入口.bat>` 捕获真实输出，区分 CMD 编码/引号解析失败、PowerShell 脚本错误和被调用程序自身失败。已验证事故记录见 `development/docs/已归档/模块根目录脚本整理-2026-09-01.md`。

## 交接

- 任务使用 `development/docs/协作/任务模板.md`。
- 跨 Codex、WorkBuddy 或其他代理软件协作时，使用仓库内任务单和报告传递状态，不依赖代理之间共享内部对话。
- 执行和验收规范见 `development/docs/协作/代理执行规范.md`。
